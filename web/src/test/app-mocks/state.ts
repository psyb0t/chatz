// Test stub for SvelteKit's $app/state. Only the shape the components read is
// provided; unit tests here target stores/parsers, not the routed pages.
export const page = {
  params: {} as Record<string, string>,
  url: new URL("http://localhost/"),
};
